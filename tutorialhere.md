this will work in 2026-01-01
This is the tutorial of how to upgrade and update ubuntu in just 3 commands
The first command is: sudo add-apt-repository ppa:jonathanhildingsson/latestupdate
The second command is: sudo apt install latestupdate
and the third (last one) is the one that does the work: latestupdate
this is the early one how to do it 
so the first step is to install the updateandupgrade_ubuntu.sh
the second step is to type: chmod +x updateandupgrade_ubuntu.sh
in the terminal and cd to where you installed it.
The final step is to run ./updateandupgrade_ubuntu.sh

